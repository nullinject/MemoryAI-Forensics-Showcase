# 技术白皮书 (Technical Design Specification)

**项目名称**: MemoryAI-Forensics (基于 LangGraph 状态图工作流的智能化数字取证平台)

本白皮书旨在阐述项目的核心架构设计思路、关键难点攻克机制以及系统工作流引擎的实现原理。

---

## 一、 为什么选择 LangGraph 作为核心驱动？

在早期的内存取证自动化尝试中，传统的线性脚本或简单的链式调用（Chain）往往面临以下痛点：
1. **取证过程的非线性**：真实的取证调查是一个动态的过程，例如“发现可疑网络连接 -> 溯源对应进程 -> 提取进程内存 -> 进行恶意代码扫描”。这种带有条件分支、循环甚至人工介入的流程，很难用线性的代码优雅实现。
2. **状态管理的复杂性**：在长周期的分析中，不同阶段产生的数据（如 OS画像结果、进程列表、网络拓扑）需要被安全地缓存和传递。

**架构设计决策**：
为此，我们引入了 `LangGraph` 状态图（StateGraph）引擎。它将取证流程抽象为一张有向图（Graph），每个节点（Node）代表一个取证动作（如初始化、策略分流、执行猎杀），图的边（Edge）代表动作之间的条件路由。
- **状态持久化 (Checkpointing)**：利用 SQLite 实现 StateGraph 的内存数据留存，这使得系统能够在分析中断时恢复，甚至支持未来加入“人类在环 (Human-in-the-loop)”的专家确认机制。
- **流式追踪 (Streaming)**：LangGraph 提供细粒度的 `astream` 推送，使得我们可以将后端的每一步状态变更（如“正在提取进程快照”）通过 SSE (Server-Sent Events) 实时推送给前端仪表盘，极大提升了用户体验。

---

## 二、 Volatility 引擎的异常护城河设计

`Volatility 3` 是业界标杆，但其痛点也是显著的——极度依赖精确的符号表（ISF）。而在实战中，面对长尾的 Linux 内核版本，经常会因为缺少匹配的 JSON 符号表而导致整个分析管线崩溃。

### 1. Auto-ISF 自愈机制
我们在中间件层实现了一套“降级与自愈”策略：
1. **特征探测**：先利用轻量级探针提取目标镜像的内核 banner（例如 `Linux version 6.6.117-45.1.x86_64`）。
2. **精准嗅探**：如果 Volatility 3 抛出 `SymbolError`，系统不会报错退出，而是触发 `auto_symbol` 中断。
3. **动态拉取**：探针会自动根据 banner 解析所需的文件名，前往预先配置的 GitHub 远端仓库自动拉取 `xxx.json.xz`。
4. **回旋重试**：符号表就位后，StateGraph 回滚状态，重新拉起 Volatility 引擎完成分析。

这种设计将原本需要人工介入数小时的“找符号、下符号、配置路径”的体力活，压缩到了 10 秒以内的自动化流程中。

### 2. SmartDumper 三级容错提取策略
从内存中提取恶意样本是“交叉验证”阶段的核心。单点依赖极易失败，因此我们设计了 `SmartDumper` 级联降级器：
- **Level 0 (最优解)**：尝试 `procdump`。优点是能提取完整的 PE/ELF 结构，方便逆向。若失败（如 PE 头被抹除），则降级到 Level 1。
- **Level 1 (妥协解)**：尝试 `vaddump`，全量 Dump 进程的虚拟地址描述符（VAD）空间。若由于内存损坏而失败，则降级到 Level 2。
- **Level 2 (兜底解)**：尝试 `malfind --dump`。直接放弃 PE 重建，仅在一片废墟中扫描并提取含有 `RWX` (读写执行) 权限的匿名注入代码段，这是无文件挖矿木马最爱潜伏的地方。

---

## 三、 数据清洗与 LLM 幻觉抑制

大模型很容易在面对海量、充满噪音的系统底层数据时产生“幻觉”或超出 Context Window 限制。

**架构设计决策**：在数据送入 `Phase 2 (策略分发)` 和 `Phase 5 (报告生成)` 的 Agent 之前，必须经过一层严格的“数据清洗管道 (Data Filter Pipeline)”。

1. **网络连接降噪**：取证工具会拉取内存中存在的所有 TCP 状态。过滤器会自动丢弃海量的 `TIME_WAIT` 和挂起的 `CLOSE_WAIT` 连接，优先把状态为 `ESTABLISHED` 的公网（非 `127.0.0.x`, `192.168.x.x`）连接置顶。
2. **白名单进程折叠**：对于如 `svchost.exe`、`systemd` 这种动辄几十个的系统常驻进程，过滤器使用基线白名单将其“折叠”，只有当其子进程异常（如 `svchost.exe` 派生了 `cmd.exe`）时才展开暴露给 LLM。
3. **高级逃逸感知 (DKOM 猎手)**：
    不依赖单一工具的结论。通过对比 `pslist`（通过遍历活跃进程链表提取）与 `psscan`（通过池标签暴力扫描物理内存提取）的**差集**，系统能自动定位使用了 DKOM (Direct Kernel Object Manipulation) 技术摘除链表来隐藏自身的 Rootkit 进程，并将该结论以高危警报 (CRITICAL) 的形式喂给 LLM，确保研判焦点不被模糊。

---

## 结语

MemoryAI-Forensics 不仅仅是一个“套壳” LLM 的简单脚本，而是从底层的执行器容错、中层的状态机流转，到上层的 LLM 意图对齐，构建了一套完整的、健壮的防御架构。系统的设计始终围绕“在混沌的数据中自动化提取高质量证据”这一核心理念展开。
